local options = {
  ensure_installed = { "lua", "vimdoc", "python", "javascript", "typescript", "markdown", "markdown", "markdown_inline", "tsx" },

  highlight = {
    enable = true,
    use_languagetree = true,
  },

  indent = { enable = true },
}

return options
